{% extends 'template.html.twig' %}

{% block title %}Login{% endblock %}
{% block stylesheets %} 
 <link rel="stylesheet" type="text/css" href="{{ asset('templates/login.css') }}" />

{% endblock %}
{% block body %}
 <section class="login">
    {% if logged == null %}

    <div class="login__baner banner__div row">
            <div class="login__h2 banner__text"><h2>Zaloguj się</h2></div>
    </div>

    <div class="container login__form form">
        <div class="form__panel">
            <form action="login" name="uzytkownicy" method="POST">
                <label class="form__field" for="username ">Nazwa użytkownika:</label>
                <input type="text" id="username" name="username" pattern="^[a-zA-Z][a-zA-Z0-9-_\.]{1,20}$" required>
                <label class="passwd form__field" for="password">Hasło: <span class="passwdInf" style="font-size:14px;">&#9432</span><span class="regextext"> Number<br> Upper case<br> Lower case <br> Min. 8 chars<br> Special character</span></label> 
                <input type="password" id="password" name="password" pattern="(?=^.{8,}$)((?=.*\d)(?=.*\W+))(?![.\n])(?=.*[A-Z])(?=.*[a-z]).*$" required>
                <div class="error_form">{% if error is defined %}{{ error }} {% endif %}</div>   
                <div class="form__submit">
                    <label class="form__check row">
                        <input type="checkbox" class="-hide form__checkbox"> 
                        <span class="form__checkboxMask"></span>
                        <span>Zapamiętaj mnie!</span>
                    </label>
                    <input class="form__button btn" type="submit" value="Zaloguj">
                </div>
            </form>
        </div>
    </div>

    {% else %}
    <div class="login__baner banner__div row">
            <div class="login__h2 banner__text"><h2><a {#id="logout" onmouseover="mouseover()" onmouseout="mouseout()"#} href="/home/logout">Wyloguj się</a></h2></div>
    </div>
    {% endif %}
</section>
{% endblock %}

 {% block javascripts %}
 {# <script> 
    function mouseover() 
    { 
        document.getElementById("logout").style.color = "#eba951"; 
    } 
                    
    function mouseout() 
    { 
        document.getElementById("logout").style.color = "#000000"; 
    } 
  </script>  #}
{% endblock %}